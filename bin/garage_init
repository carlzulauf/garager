#!/usr/bin/env ruby

# INSTALL: sudo update-rc.d -f garage_init start 4

DIR="/home/pi/garager"

begin
  case ARGV.first
  when /start/
    Dir.chdir(DIR)
    exec "daemon bin/client"
  end
rescue => e
  puts %[#{e.class.to_s}\n#{e.backtrace.join("\n")}]
end
